  <script>
    function reloadPage() {location.reload()}
  </script>

<% if user_signed_in? %>

  <h3>Good job! Here's your reward: </h3>
  <div class="row">
    <div class="span8">
      <%= render 'rewards/new_reward_partial' %>
    </div>

    <div class="span4">
      <div class="big-reward-button">
       <p><a href="#" class="btn btn-large" value="Get Another Reward" onclick="reloadPage()">Get Another Reward <i class="icon-heart"></i></a></p>
      </div>
    </div>
  </div>

<div class="accordion" id="accordion2">
  <div class="accordion-group">
    <div class="accordion-heading">
      <a class="accordion-toggle" data-toggle="collapse" data-parent="#accordion2" href="#collapseOne">
        Edit the Rewards here
      </a>
    </div>
    <div id="collapseOne" class="accordion-body collapse">
      <div class="accordion-inner">
          <table>
            <tr>
              <th>Name</th>
              <th>Description</th>
              <th>Size</th>
              <th></th>
              <th></th>
              <th></th>
            </tr>

          <% @rewards.each do |reward| %>
            <tr>
              <td><%= reward.name %></td>
              <td><%= reward.description %></td>
              <td><%= reward.size %></td>
              <td><%= link_to 'Show', reward %></td>
              <td><%= link_to 'Edit', edit_reward_path(reward) %></td>
              <td><%= link_to 'Destroy', reward, method: :delete, data: { confirm: 'Are you sure?' } %></td>
            </tr>
          <% end %>
        </table>
        <%= link_to 'New Reward', new_reward_path %>
      </div>
    </div>
  </div>
</div>

<% else %>

  <div class="container-narrow" align="center">
    <h1>It's never been more fun to do the right thing</h1>
    <p>Did you just get some stuff done? Did your daily meditation? Drank a glass of water? Avoid looking at Hacker News for the first hour of your day? Great job! Here's your reward:</p>
    <div class="big-reward">
      <%= Reward.test_redeem.description %>
    </div>
    <div class="big-reward-button">
       <p>
        <a href="#" class="btn btn-primary btn-large" value="Get Another Reward" onclick="reloadPage()">Get Another Reward <i class="icon-heart"></i></a>
        <%= link_to "Sign Up to create your own rewards", new_user_registration_path, class: "btn btn-large" %>
       </p>
     </div>

</div>

<% end %>
